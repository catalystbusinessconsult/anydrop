{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main CBC LAN Share window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-minimize",
    "fs:allow-write-file",
    "fs:allow-read-file",
    "fs:scope-appdata",
    "shell:allow-open",
    {
      "identifier": "shell:allow-execute",
      "allow": [{ "name": "binaries/cbc-coordinator", "sidecar": true }]
    },
    "autostart:default"
  ]
}
