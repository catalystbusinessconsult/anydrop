node_modules/
dist/
build/
*.log
.env
.env.local
.DS_Store

# Electron packaged output
desktop/release/
desktop/electron/main.bundle.cjs

# local one-off scratch tooling (mkcert binary, icon-gen script + deps) —
# not part of the shipped repo
.tools/

# certs — except the shared office-LAN cert/key, which IS committed
# intentionally so every laptop and the CI build trust the same identity
# without per-machine setup (see desktop/README.md "Local HTTPS")
*.pem
*.crt
*.key
!web/certs/cert.pem
!web/certs/key.pem

# test output
coverage/
